# X / Twitter — Thread (6 tweets)

## Tweet 1/6
I built a 4-layer routing stack that pushes ~65% of AI agent tasks away from the cloud.

Not by making models smarter — by making the pipeline cheaper.

Layer 1: micro-NN (numpy, ~5µs, 0 tokens)
Layer 2: deterministic NLP (stdlib, 0 tokens)
Layer 3: local LLM (0¢)
Layer 4: cloud (only the hard stuff)

## Tweet 2/6
The 4 micro-NNs are distilled from realistic data (not np.random). Each is ~2KB:

• binary_router — local or cloud? (100% acc)
• effort_classifier — how hard is this task? (98%)
• anomaly_detector — is this call normal? (100%)
• error_classifier — what went wrong?

All 4/4 grounded with provenance tracking.

## Tweet 3/6
The repo ships 78 skills:

→ Code audit (dead code, secrets, complexity)
→ Auto-fix with verification
→ Red team adversarial pipeline
→ Context profiler (52% of my Hermes context is runtime overhead!)
→ MCP gateway (20+ tools, any agent)
→ Security scanner (taint/data-flow, CWE)

## Tweet 4/6
The `botte` CLI wrapper compresses terminal output by 60-99%.

`botte cargo test` instead of `cargo test`
→ 90% fewer tokens entering agent context.

Zero-config. Just prefix any command.

## Tweet 5/6
By the numbers:
• 138 tests, 0 failures
• 0 heavy ML deps (numpy + stdlib)
• GPG-signed commits (RSA 4096)
• GitHub Discussions open
• MIT license

github.com/zedarvates/botte-secrete

## Tweet 6/6
Why I built this: most "local AI" tools are API wrappers with a green badge. I wanted something that genuinely reduces cloud dependency — 4 distilled micro-NNs, deterministic NLP, local LLM routing. Each layer abstains → escalates. Cheapest first.

Curious: what are YOU using for local↔cloud routing in your agent pipelines?
