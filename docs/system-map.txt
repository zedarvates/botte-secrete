botte-secrète  (the system)
│
├─ SENSE — understand project / cluster / task
│     directives_audit, fallow_like, infra_advisor, llm_backends, metrics, skill_finder
├─ DECIDE — route the work, cheapest capable
│     auto_router↑, cluster, conductor, preflight, tiered_router↑
├─ ACT — do the work, local-first
│     app_test, cardinal↑, docgen↑, ingest, llm_mcp, mousquetaires↑, prompt_improver
├─ REMEMBER — capitalise (compounding)
│     cache, code_fingerprint, diff_language, loader, ultra_compact, vector_protocol
├─ GOVERN — consistency & cost control
│     capabilities, checkup, clarification, skill_project_optimizer
└─ DEPLOY — wire into projects & measure
      bootstrap

↑ = can escalate to a cloud model; everything else is local-first.
(generated by: python -m skills.capabilities.cli map)
